import styled from 'styled-components';
import Layout from 'antd/lib/layout';

export const LayoutUI = styled(Layout)`
  min-height: 100vh;
`;

export const PageUI = styled(Layout)`
  margin: 64px auto 30px;
  width: 100%;
  max-width: 1480px;
`;
