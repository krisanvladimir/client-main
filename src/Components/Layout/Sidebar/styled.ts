import styled from 'styled-components';
import Layout from 'antd/lib/layout';

const { Sider } = Layout;

export const SidebarUI = styled(Sider)`
  background: #fff;
  position: fixed;
  height: 100vh;
`;
